node_modules/
dist/
coverage/
*.tsbuildinfo

# haido's own database (knowledge travels via docs/memory/ pack instead)
.haido/

# local-only agent settings
.claude/settings.local.json

# probe runtime output (schema excerpts live in FINDINGS.md)
experiments/hooks-probe/log.jsonl
